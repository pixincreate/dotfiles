-- secrets.lua.example
-- Copy this file to secrets.lua and add your actual API keys
-- This file is gitignored to keep your secrets safe

local secrets = {
    -- API key for Llama (optional)
    -- Used for AI-powered features if available
    llama_api_key = "your-llama-api-key-here",
    
    -- Add other API keys or secrets as needed
    -- gemini_api_key = "your-gemini-api-key-here",
    -- example_api_key = "your-api-key-here",
}

return secrets